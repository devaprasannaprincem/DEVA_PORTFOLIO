[build]
  command   = "npm run build"
  publish   = "dist"

# SPA redirect — every route serves index.html so React handles routing
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
